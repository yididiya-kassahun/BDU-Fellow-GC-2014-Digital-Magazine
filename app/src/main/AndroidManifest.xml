<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vlm.gc2014digitalmag">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GC2014DigitalMag">
        <activity android:name=".ZenzelimaActivity"></activity>
        <activity android:name=".YibabActivity" />
        <activity android:name=".PedaActivity" />
        <activity android:name=".PolyActivity" />
    </application>

</manifest>